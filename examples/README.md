# Examples

Examples used by agents should live here.

- `good/`: valid target examples.
- `bad/`: intentionally invalid examples.
- `before_after/`: examples showing how rough input becomes normalized output.

Example files should be small and focused. Prefer one concept per file so agents
can use them as retrieval and comparison anchors.
