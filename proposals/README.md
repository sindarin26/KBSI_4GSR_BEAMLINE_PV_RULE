# Proposals

This directory contains proposed changes to the active rulebooks.

Use proposals when one or more exceptions show that the rulebooks need to grow.
Do not treat a proposal as active until it is reviewed and promoted into
`rules/`.

Rule-change proposals should live under:

```text
proposals/rule_changes/
```

