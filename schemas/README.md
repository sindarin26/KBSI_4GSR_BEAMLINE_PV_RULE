# Schemas

Machine-readable schemas live here.

Current files:

- `pv_registry.seo_v3.yaml`: informal active registry contract for SEO_V3.
- `pv_registry.seo_v2.yaml`: legacy SEO_v2 registry contract retained for
  migration reference.
- `pv_registry.v0.yaml`: legacy informal v0 registry contract retained for
  historical outputs and migration reference.
