# Schemas

Machine-readable schemas live here.

- `database_pool.seo_v3.yaml`: informal active database-pool contract for
  SEO_V3 source rows, decision overlays, abbreviation review records, and the
  review workbench.

Stricter validation may replace or extend this informal contract later. SEO_v2
and v0 schema files were removed during the 2026-06-02 hard-reset alignment.
